# VacancyPilot Product Development Plan

Status: HISTORICAL IMPLEMENTATION PLAN / superseded as active execution guidance by accepted Application Ops R5 + dogfood freeze
Source of truth: `docs/Техническое заданиеV.1.md` v1.1 FINAL

Current-state pointer:

- Current product/runtime truth: [`docs/project-memory/CURRENT_STATE.md`](../project-memory/CURRENT_STATE.md)
- Current implementation evidence: [`docs/development/application-ops/IMPLEMENTATION_STATUS.md`](application-ops/IMPLEMENTATION_STATUS.md)
- Daily-use gate: [`docs/development/application-ops/r5/R5_DAILY_USE_READINESS.md`](application-ops/r5/R5_DAILY_USE_READINESS.md)
- Current roadmap and next decision gate: [`docs/ROADMAP.md`](../ROADMAP.md)

The decomposition below is retained for archaeology and traceability. It is not an active implementation queue while feature development is frozen.

Implementation note:

- spec-level `Phase 3 — Workflow Queue` was partially absorbed into the enlarged implementation Phase 2 pack (`ITER-037` / `ITER-038`);
- spec-level `Phase 4 — Guided Apply Labs` and the local workflow-assist layer landed through `ITER-039`..`ITER-042`;
- the dependency/toolchain maintenance and security-closure packs landed after that product work;
- the HR communication pack landed through `ITER-044` and `ITER-045`;
- the post-audit reliability/scoring hardening pack landed through `ITER-058`;
- the private release readiness pack (`ITER-063` / `ITER-064`) landed on 2026-06-21;
- the former AI/UI follow-up sequences are historical planning context, not current work;
- a post-polish runtime stabilization pack is prepared as `EPIC-35` (`ITER-069`..`ITER-071`) for defects confirmed by manual QA/screenshots;
- a visual consistency consolidation pack is prepared as `EPIC-36` (`ITER-072`..`ITER-075`) from the 2026-06-21 runtime UX audit;
- a non-duplicated audit-closure follow-up pack is prepared as `EPIC-37` (`ITER-076`..`ITER-078`) for repo-local quality/trust items from the 2026-06-22 full audit, while the duplicated runtime/UI findings remain intentionally mapped to `EPIC-35` and `EPIC-36`;
- the Codex-led `EPIC-38` pack (`ITER-079`..`ITER-081`) delivered HH Visual Triage / Search Highlights MVP and its follow-up search-card discovery hardening;
- `n8n` remains deferred until its permission model is explicitly reopened.

## Decision

The master specification is frozen for implementation start. Do not keep expanding it before Phase 0. New learnings go into implementation notes or issue backlog unless they change product boundaries, permissions, data model, or external data flows.

## Development Strategy

Build the product from the safest inner core outward:

1. Foundation: WXT, MV3, TypeScript, React, test harness.
2. Local contracts: typed models, Dexie schema, migrations, settings.
3. HH read layer: vacancy parser, fixtures, passive status extraction.
4. Local value: tracker, status history, scoring, export.
5. UI shell: popup, side panel, dashboard.
6. AI: payload preview, redaction, cache, Strict Privacy.
7. Cover letters: generation, edit, save, copy.
8. n8n: opt-in webhook, HMAC, retry, event log.
9. Release hardening: privacy/safety tests, manifest audit, manual QA.

Search badges, queue, guided apply, HR communication, and multi-site support are deliberately out of Phase 0/1 until the Core is stable.

## Working Model

One iteration should be small enough for one focused AI autopilot run and one human review.

Recommended unit:

```text
one iteration -> one branch or local change set -> one commit -> one push
```

For now, commits can go to `main` while the project is private and early. Once implementation begins, prefer short-lived branches:

```text
codex/iter-001-wxt-scaffold
codex/iter-002-domain-models
```

## Source Priority

When documents conflict:

1. `docs/Техническое заданиеV.1.md`
2. `AGENTS.md`
3. `docs/development/*.md`
4. `docs/search/*.md`

If a recommendation from `docs/search` conflicts with safety boundaries, reject it unless the master spec is explicitly updated.

## Definition Of Ready

An iteration is ready for autopilot when it has:

- a narrow goal;
- allowed files or target folders;
- explicit non-goals;
- safety constraints;
- acceptance criteria;
- validation commands;
- expected commit message.

## Definition Of Done

Every implementation iteration must satisfy:

- no forbidden HH automation;
- no broad permissions;
- no secrets or keys committed;
- tests or fixture checks where practical;
- typecheck/build green once the project has scripts;
- docs updated if contracts or behavior changed.

## Phase Gates

### Gate A: Start Coding

Ready after this decomposition pack exists and is committed.

### Gate B: Phase 0 Complete

Required:

- installable extension skeleton;
- MV3 manifest generated by WXT;
- no broad permissions;
- parser fixtures directory;
- base tests running;
- no network calls to HH from content scripts.

### Gate C: Phase 1 Release Candidate

Required:

- vacancy parser passes 50+ fixtures;
- local tracker works;
- scoring works without AI;
- AI can be fully disabled;
- n8n can be fully disabled;
- export/delete all work;
- privacy/safety tests pass;
- manual QA in Chrome and one additional Chromium browser.

## Immediate Next Actions — superseded current guidance

The former `ITER-060`..`ITER-062` and UI-pack sequence is preserved above for historical traceability. Current actions are real vacancy/application dogfood, repeated friction and quality evidence collection, and immediate hotfixes only for the criteria in [`R5_DAILY_USE_READINESS.md`](application-ops/r5/R5_DAILY_USE_READINESS.md). Do not start AOPS-14 or full AOPS-15 by default; choose the next feature milestone only from evidence.
