# Prompt AOPS-00 — Baseline, Scope and Contract Freeze

Implement only epic `AOPS-00` in the open VacancyPilot repository root.

## Goal

Adopt the Application Ops MVP as a repository-native specification, record the
real implementation baseline, resolve architecture decisions, and freeze the
first API/data contract without changing runtime product behavior.

## Read first

1. `AGENTS.md`
2. `docs/Техническое заданиеV.1.md`
3. `docs/development/00-product-development-plan.md`
4. `docs/development/01-epics.md`
5. `docs/development/02-iteration-map.md`
6. `docs/development/03-autopilot-workflow.md`
7. `docs/development/CODEX-RUNTIME-BRIEF.md`
8. Canonical source specification:
   `docs/mvp/VACANCYPILOT_APPLICATION_OPS_MVP.md`

## Preconditions

- Worktree must be clean.
- Branch must be `codex/application-ops-mvp`.
- Prepared predecessor `e36a067ae4e8ef931bf0f151712016cb4dbce47e` must be an
  ancestor of HEAD, and the repo-native pack/spec must be present. If not, do
  not reset or switch; report the actual HEAD and stop for review.
- Do not reset to historical commit
  `71ab48c48376a1e7b44ed0733fdc9aa435f39e76`.
- Verify whether that commit is an ancestor of current HEAD and record the
  result.
- Record the `v4.0.0` tag as its exact object ID or `ABSENT`. Preserve that
  state; do not create, move, force-update, or delete the tag.
- If the canonical specification is unavailable or its hash differs from the
  pinned value, stop without reconstructing or rewriting it.

## Required work

1. Verify the already imported canonical MVP document:

```text
docs/mvp/VACANCYPILOT_APPLICATION_OPS_MVP.md
```

Its required SHA-256 is:

```text
5ABDC4B3AE59029BB3159CA8F3FE2D82C58B37660396D53F3904FE0F32662C08
```

Record the actual hash and verify it matches. Do not rewrite the document.

2. Add:

```text
docs/development/application-ops/
├─ README.md
├─ IMPLEMENTATION_STATUS.md
├─ API_CONTRACT_V1.md
├─ DATA_MODEL_V1.md
└─ adr/
   ├─ ADR-001-local-companion.md
   ├─ ADR-002-sqlite-dexie-authority.md
   ├─ ADR-003-openapi-contract-source.md
   ├─ ADR-004-hh-official-read-only-boundary.md
   ├─ ADR-005-engine-package-privacy.md
   └─ ADR-006-ai-provider-boundary.md
```

3. Record:

- actual start commit, branch, date and baseline command results;
- existing extension capabilities that must remain intact;
- P0/P1/non-goal boundary;
- explicit dependency sequence `AOPS-00…AOPS-18`;
- authority rule: SQLite canonical only in Ops Mode; Dexie canonical in
  Standalone Mode and cache/outbox after migration;
- append-only events/runs/versions/snapshots and transactional current status
  projection;
- OpenAPI generated by FastAPI as canonical local API contract;
- versioned `/api/v1` response/error envelope and request ID;
- loopback-only companion;
- official HH API only; no extension-side hidden HH requests;
- DeepSeek in Zed is a coding tool, not automatically a product provider.

4. In ADR-005 choose the privacy-safe default:

- real V4 candidate knowledge is not committed automatically;
- repository contains loader, schemas, installer/import tooling and synthetic
  fixtures;
- real package is installed locally from a user-approved canonical artifact;
- an explicit private-repository decision is required before vendoring facts.

5. Reconcile this pack with existing development docs without renumbering or
rewriting unrelated completed epics. Add a concise link from the development
README or another appropriate index.

6. Create an acceptance traceability table mapping every section 25 MVP
criterion to its planned AOPS epic.

## Non-goals

- no companion code;
- no package dependency changes;
- no manifest/permission changes;
- no Dexie schema changes;
- no Application Engine runtime edits;
- no HH or AI calls;
- no cleanup of unrelated docs.

## Acceptance criteria

- canonical MVP spec exists and its SHA-256 matches the pinned value;
- historical baseline ancestry result and actual start commit are recorded;
- six ADRs have status, context, decision, consequences and rejected options;
- API contract lists endpoints, auth category, idempotency expectations and
  error envelope;
- data model covers the MVP tables and invariants without secrets;
- all section 25 criteria map to an epic;
- existing product behavior and dependencies are unchanged;
- baseline test commands actually pass or exact pre-existing failures are
  documented without being modified.

## Validation

```powershell
git diff --check
pnpm verify
pnpm test:release
```

Also verify package manifests and extension permissions are unchanged.

## Handoff

Follow `docs/development/application-ops-pack/ZED_SESSION_START.md`. Do not commit or push.

Expected reviewed commit message:

```text
docs: adopt Application Ops MVP contracts
```
