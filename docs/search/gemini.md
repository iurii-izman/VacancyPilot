```python
spec_content = """# Техническая спецификация и продуктовая стратегия: CareerSignal HH Copilot (MVP)

## 1. Executive Summary
Продукт **CareerSignal HH Copilot** представляет собой локально-ориентированное (local-first) браузерное расширение для автоматизации и повышения качества поиска работы на платформе HH.ru с возможностью дальнейшего масштабирования на международные платформы (LinkedIn, Indeed и др.).

В отличие от агрессивных спам-ботов, создающих избыточную нагрузку на платформы и ухудшающих конверсию из-за низкого качества сопроводительных писем, данный проект базируется на концепции **User-in-the-loop Copilot** (ассистент с полным контролем пользователя). Продукт снижает когнитивную нагрузку на соискателя, автоматизирует рутинный сбор данных, предоставляет гибридный скоринг вакансий (Rule-based + AI по запросу) и обеспечивает генерацию кастомизированных сопроводительных писем на основе профилей кандидата без обязательного использования выделенного бэкенда (модель Bring-Your-Own-Key - BYOK).

---

## 2. Сильные стороны концепции
* **Отказ от полной автоматизации откликов в MVP:** Это защищает учетную запись пользователя от моментального бана системами антифрода HH.ru и гарантирует, что каждый отклик остается осознанным.
* **Гибридная модель скоринга:** Первичный rule-based анализ выполняется мгновенно и бесплатно на клиенте, экономя токены и время. Глубокий AI-анализ вызывается пользователем точечно для релевантных вакансий.
* **Архитектура Local-first + BYOK:** Использование `Dexie.js` (IndexedDB) и прямых запросов к API провайдеров (OpenAI / DeepSeek / OpenRouter) минимизирует затраты на инфраструктуру для разработчика и полностью снимает юридические риски, связанные с хранением персональных данных (ФЗ-152, GDPR) на сторонних серверах.
* **Менеджер профилей (Мультипрофильность):** Решает реальную боль T-shaped специалистов и синьоров, которые вынуждены адаптировать резюме под разные домены (например, *AI PM* vs *Technical Analyst* vs *n8n Automation Lead*).

---

## 3. Опасные и спорные моменты (Критический аудит)
* **Высокая зависимость от DOM-структуры HH.ru:** Попытки парсить вакансию через стандартные CSS-классы (`.vacancy-title`, `.bloko-button`) приведут к регулярным поломкам расширения. HH.ru часто проводит A/B тесты и обновляет/обфусцирует верстку.
* **Риск блокировок при парсинге поисковой выдачи (MVP-2):** Если расширение будет отправлять фоновые HTTP-запросы на страницы вакансий из общего списка выдачи для получения `description` или `skills`, Cloudflare/Qrator со стороны HH.ru заблокирует IP-адрес пользователя за аномальную активность (DDoS-поведение).
* **UX-перегрузка в интерфейсе:** Наличие десятка микро-переключателей (без emoji, без markdown, ограничение 500/1000 символов, выбор тональности) непосредственно в боковой панели расширения перегрузит интерфейс и усложнит принятие решений.
* **Ограничения Service Worker в Manifest V3:** Фоновые скрипты (`background.ts`) в MV3 могут засыпать. Длительные стриминговые запросы к LLM или тяжелые цепочки обработки могут прерываться, если архитектура не предусматривает сохранение промежуточного состояния.

---

## 4. Исправленная продуктовая стратегия
1.  **Парсинг через внутреннее состояние (Data State Extraction):** Вместо парсинга сырого HTML-дерева расширение должно искать на странице тег `<script>` с глобальным стейтом приложения (например, `window.__INITIAL_STATE__` или `id="hh-state"`). Там содержатся чистые, структурированные JSON-данные вакансии, предоставляемые бэкендом самого HH.ru для SSR (Server-Side Rendering). Это исключит 90% поломок парсера.
2.  **Ленивый скоринг в поисковой выдаче:** В рамках MVP-2 на странице поиска анализируются *только те данные, которые уже присутствуют в сниппете карточки* (название, компания, компенсация, базовые теги). Фоновое фетчинг полных страниц вакансий без клика пользователя строго запрещен.
3.  **Абстракция настроек генерации ИИ:** Микро-настройки (emoji, длина, язык) переносятся в настройки конкретного Профиля Кандидата в виде системных промптов или пресетов ("Строгий стиль", "Краткий текст для Telegram", "Стандартный HH"). Интерфейс расширения на странице вакансии должен оставаться чистым (одна кнопка «Сгенерировать»).
4.  **Изоляция через Shadow DOM:** Весь интерфейс расширения, внедряемый на страницы HH.ru, должен быть изолирован в Shadow DOM. Это гарантирует, что глобальные стили HH.ru не исказят UI расширения, а скрипты оптимизации интерфейса HH.ru не нарушат логику работы Copilot.

---

## 5. MVP Scope (Декомпозиция по версиям)


```

```text
File generated successfully: careersignal_hh_copilot_mvp_spec.md


```

[ MVP-1: Single Page & Core ] ──> [ MVP-2: Search List View ] ──> [ MVP-3: Form Autofill ]
│
[ MVP-5: Chat & Follow-ups ]  <── [ MVP-4: Queue & Kanban ]  <──────────────┘

```

### MVP-1: Core & Single Page (Базовый рабочий продукт)
* **Функционал:** Парсинг открытой страницы вакансии (через JSON state), базовый rule-based скоринг, ручной вызов AI-анализа (BYOK), генерация сопроводительного письма в интерфейсе расширения, копирование письма в буфер обмена кнопкой, сохранение вакансии в локальную базу со статусом `new`/`applied`, отправка вебхука в n8n при сохранении/отклике.
* **UI:** Боковая панель (Sidebar), внедряемая на страницу вакансии через Shadow DOM. Страница настроек (Options page) для ввода API-ключей, URL вебхука и создания профилей.

### MVP-2: Search List View (Оптимизация выдачи)
* **Функционал:** Инжекция бейджей статусов (`viewed`, `applied`, `blacklist`) в карточки вакансий на страницах поисковой выдачи HH.ru на основе сопоставления ID вакансий с локальной БД Dexie. Легковесный rule-based скоринг вакансий прямо в списке (по названию и навыкам из сниппета).
* **UI:** Компактные индикаторы и скоринг-баллы, аккуратно встроенные в верстку карточки вакансии HH.ru.

### MVP-3: Semi-automatic Apply (Полуавтоматический отклик)
* **Функционал:** При нажатии пользователем кнопки "Откликнуться" на HH.ru, расширение отслеживает появление модального окна отклика (через `MutationObserver`), автоматически выбирает нужное резюме из списка (если применимо) и вставляет сгенерированное сопроводительное письмо в текстовое поле `textarea` с симуляцией нативных событий ввода (`InputEvent`).
* **UI:** Кнопка быстрого выбора профиля и генерации/вставки прямо внутри модального окна HH.ru.

### MVP-4: Queue & Funnel Management (Управление очередью)
* **Функционал:** Полноценный Dashboard внутри расширения (вкладка браузера). Возможность добавлять вакансии в "Очередь на отклик". Потоковая обработка очереди: пользователь видит вакансию, сгенерированное письмо, нажимает "Отправить" (происходит полуавтоматический отклик в фоне/соседней вкладке), статус обновляется, загружается следующая вакансия из очереди.
* **UI:** Канбан-доска воронки откликов (New -> Saved -> Letter Ready -> Applied -> Interview -> Rejected) в Dashboard.

### MVP-5: Comms Automation & Analytics (Коммуникации)
* **Функционал:** Парсинг страницы сообщений и чатов HH.ru. AI-классификация ответов HR (приглашение, отказ, уточнение данных). Генерация вариантов ответов на сообщения HR. Выгрузка расширенной аналитики (конверсия из отклика в ответ по разным профилям) в CSV/JSON. Отправка нотификаций о сообщениях в Telegram через n8n.

---

## 6. Архитектура расширения

* **Стек:** Manifest V3, WXT (современный фреймворк для разработки расширений с поддержкой TypeScript, Vite и горячей перезагрузки), React или Solid.js (для UI), TailwindCSS (стилизация), Dexie.js (обертка над IndexedDB).
* **Компоненты системы:**
    1.  **Background Service Worker (`background.ts`):** Единственная точка выполнения сетевых запросов к AI API и вебхукам n8n. Это предотвращает проблемы с CORS (Cross-Origin Resource Sharing) на страницах HH.ru. Отвечает за долгоживущие транзакции базы данных и системные уведомления.
    2.  **Content Script `job-page.ts`:** Инжектирует Shadow DOM на страницу вакансии. Извлекает JSON-стейт страницы, обменивается сообщениями с `background.ts` для выполнения AI-запросов и сохранения данных в БД.
    3.  **Content Script `search-page.ts`:** Легковесный скрипт, сканирующий поисковую выдачу, сопоставляющий ID вакансий с локальной БД и инжектирующий UI-бейджи.
    4.  **Dashboard / Options Page (`index.html`):** Полноценное Single Page Application (SPA), открывающееся в отдельной вкладке. Предоставляет интерфейс управления профилями, просмотра сохраненных вакансий и настройки системных параметров.

---

## 7. Data Model (TypeScript / Dexie.js)

```typescript
// Перечисление возможных статусов вакансии в воронке
export type JobStatus =
  | 'new'
  | 'viewed'
  | 'saved'
  | 'rejected_by_me'
  | 'letter_ready'
  | 'applied'
  | 'hr_replied'
  | 'interview'
  | 'test_task'
  | 'rejected_by_company'
  | 'offer'
  | 'blacklist';

// Основная сущность Вакансии
export interface Job {
  id: string;               // Уникальный составной ID, например: "hh_12345678"
  source: 'hh.ru' | 'linkedin' | 'custom';
  sourceId: string;         // Внутренний ID платформы (например, "12345678")
  url: string;
  title: string;
  companyId: string;        // Ссылка на сущность Company
  companyName: string;
  salaryMin?: number;
  salaryMax?: number;
  currency?: string;
  experience?: string;      // Требуемый опыт (например, "1–3 года")
  employmentMode?: string;  // Полная занятость, удаленная работа и т.д.
  descriptionText: string;  // Очищенный от HTML тегов текст описания
  skills: string[];         // Массив ключевых навыков
  rawJsonDebug?: string;    // Полный JSON стейт страницы (сохраняется только в режиме debug)
  ruleScore: number;        // Результат rule-based скоринга (0-100)
  aiScore?: number;         // Результат AI скоринга (0-100)
  aiVerdict?: 'apply' | 'think' | 'skip';
  aiAnalysisText?: string;  // Структурированный текст анализа ИИ (плюсы, минусы, риски)
  status: JobStatus;
  createdAt: number;        // Timestamp создания записи
  updatedAt: number;        // Timestamp последнего обновления
}

// Сущность Компании для ведения черных списков и аналитики работодателей
export interface Company {
  id: string;               // Идентификатор компании, например: "hh_comp_9999"
  name: string;
  url?: string;
  isBlacklisted: boolean;
  blacklistReason?: string;
  notes?: string;
}

// Профиль кандидата (таргетинг соискателя)
export interface CandidateProfile {
  id: string;               // UUID
  name: string;             // Название профиля, например: "n8n Automation Engineer"
  targetTitles: string[];   // Целевые заголовки вакансий для скоринга
  resumeText: string;       // Текстовая выжимка опыта работы для ИИ
  keySkills: string[];      // Ключевые навыки (для rule-based матчинга)
  preferredSalaryMin?: number;
  salaryCurrency: string;
  customPromptInstructions?: string; // Кастомные инструкции для генерации писем (например: "Писать на английском")
  isDefault: boolean;
}

// Сущность отправляемых событий (Лог интеграции с n8n)
export interface IntegrationEvent {
  id: string;
  entityType: 'job_status_changed' | 'application_sent' | 'hr_message_received';
  entityId: string;         // ID связанной вакансии или сообщения
  payload: Record<string, any>;
  status: 'pending' | 'sent' | 'failed';
  errorText?: string;
  timestamp: number;
}

```

---

## 8. Scoring Model (Rule-based)

Алгоритм выполняется локально на `Content Script` сразу после парсинга данных, без обращения к сети. Итоговый балл `RuleScore` варьируется от `0` до `100`.

### Компоненты расчета:

1. **Проверка Черных Списков (Стоп-фактор):**
* Если `Company.isBlacklisted === true` $\rightarrow$ `RuleScore = 0`, статус автоматически меняется на `blacklist`.
* Если в тексте вакансии (`descriptionText`) или заголовке найдены глобальные стоп-слова пользователя (например: *"холодные звонки"*, *"crypto scam"*, *"1С"* для frontend-разработчика) $\rightarrow$ `RuleScore = 0`.


2. **Матчинг Ключевых Навыков (Вес: 40% / макс. 40 баллов):**
* Пусть $S_{vac}$ — множество навыков из вакансии, $S_{prof}$ — множество ключевых навыков выбранного профиля кандидата.
* Формула: $Score_{skills} = \left( \frac{|S_{vac} \cap S_{prof}|}{|S_{vac}|} \right) \times 40$
* *Примечание:* Если в вакансии не указаны Hard Skills в виде тегов, производится поиск по ключевым словам из $S_{prof}$ в тексте `descriptionText` (каждое совпадение добавляет по 5 баллов, в пределах 40).


3. **Матчинг Заголовка Вакансии (Вес: 30% / макс. 30 баллов):**
* Поиск вхождений строк из `Profile.targetTitles` в заголовок `Job.title` (без учета регистра).
* Полное совпадение или вхождение сильной маски (например, "React Developer" в профиле и "Senior React Developer" в вакансии) $\rightarrow$ 30 баллов.
* Частичное совпадение (хотя бы одно ключевое слово, например "Developer") $\rightarrow$ 15 баллов.
* Нет совпадений $\rightarrow$ 0 баллов.


4. **Финансовый Матчинг (Вес: 30% / макс. 30 баллов):**
* Если вилка зарплаты в вакансии не указана $\rightarrow$ дефолтное значение **15 баллов** (нейтральный фактор).
* Если указана:
* `Job.salaryMax` < `Profile.preferredSalaryMin` $\rightarrow$ 0 баллов.
* `Job.salaryMin` $\ge$ `Profile.preferredSalaryMin` $\rightarrow$ 30 баллов.
* `Profile.preferredSalaryMin` находится внутри интервала [`salaryMin`, `salaryMax`] $\rightarrow$ 25 баллов.





---

## 9. UX/UI Структура

Интерфейс должен мимикрировать под нативный современный инструмент продуктовой аналитики, избегая перегруженных элементов управления.

### 1. Боковая панель вакансии (Shadow DOM Sidebar)

* Закрепленный плавающий ярлык (Floating Action Button) с логотипом CareerSignal у правого края экрана.
* По клику выдвигается правая панель (ширина 380px).
* **Состав панели:**
* **Header:** Текущий статус вакансии (выпадающий список для быстрой смены вручную), Rule-Based Score (индикатор-кольцо: зеленый/желтый/красный).
* **Profile Selector:** Дропдаун выбора активного Профиля Кандидата (автоматически выбирается дефолтный профиль с наибольшим Score).
* **AI Analysis Block:** Кнопка `✨ Запустить ИИ-анализ`. После клика заменяется на структурированный блок:
* *Вердикт:* Откликаться / Подумать / Пропустить.
* *Списки:* 🟢 Плюсы (до 3 пунктов), ⚠️ Риски и несоответствия (до 3 пунктов).


* **Cover Letter Block:** Кнопка `✍️ Сгенерировать письмо`. Ниже рендерится текстовое поле (`textarea`), где стримится или появляется готовый текст. Кнопки под полем: `📋 Скопировать текст` и `Откликнуться со сменой статуса`.



### 2. Поисковая выдача (MVP-2 Инжекции)

* Рядом с заголовком вакансии вставляется мини-бейдж со статусом (например, серый `Просмотрено`, зеленый `Откликнулся`, красный `Блеклист`).
* Рядом с зарплатным блоком выводится маленький цифровой индикатор (например, `⚡ 85%`).

### 3. Личный Кабинет / Настройки (Options SPA)

* **Вкладка «Профили»:** Управление карточками резюме, добавление ключевых слов, желаемой зарплаты и персональных системных инструкций для ИИ.
* **Вкладка «Трекер вакансий»:** Локальная таблица/канбан всех сохраненных вакансий с возможностью фильтрации, поиска и экспорта в JSON/CSV.
* **Вкладка «Интеграции»:** Поля ввода API-ключей OpenAI/DeepSeek/OpenRouter, проверка соединения, ввод URL вебхука n8n и выбор триггеров для отправки событий.

---

## 10. AI Prompts (Шаблоны промптов)

Запросы выполняются с параметром `response_format: { "type": "json_object" }` (для моделей, поддерживающих JSON-mode, например GPT-4o, DeepSeek-V3), чтобы гарантировать стабильный парсинг.

### Промпт 1: Глубокий анализ соответствия вакансии

**Системный промпт (System):**

```text
You are an expert IT Recruiter and Technical Career Coach. Your task is to critically analyze the provided Job Description against the Candidate's Resume Profile.
Be objective and realistic. Do not flatter the candidate. Identify hidden risks, tech stack mismatches, or indicators of poor work conditions.

You must reply ONLY with a valid JSON object. Do not include any markdown formatting wrappers like ```json ... ```.

JSON Structure:
{
  "aiScore": number (integer from 0 to 100 representing overall suitability),
  "verdict": "string" (must be exactly one of these: "apply", "think", "skip"),
  "pros": ["string"], (maximum 3 strong advantages of this job for the candidate)
  "cons": ["string"], (maximum 3 key technological gaps or disadvantages)
  "risks": ["string"] (maximum 2 red flags or warning signs found in the text, e.g., overwork indicators, vague requirements)
}

```

**Промпт пользователя (User):**

```text
Candidate Profile:
- Target Position: {Profile.name}
- Core Skills: {Profile.keySkills}
- Background Summary: {Profile.resumeText}

Job Details:
- Title: {Job.title}
- Company: {Job.companyName}
- Skills Required: {Job.skills}
- Description: {Job.descriptionText}

Analyze and output JSON.

```

### Промпт 2: Генерация сопроводительного письма (Cover Letter)

**Системный промпт (System):**

```text
You are an expert career consultant helper. Generate a highly personalized, compelling, and concise cover letter (сопроводительное письмо) for the candidate applying to the specified vacancy.

Strict Constraints:
1. Language: Russian (unless the job description is written entirely in English).
2. Formatting: Plain text only. Absolutely NO markdown, NO asterisks (**), NO hashtags (#), NO emojis. Use clear line breaks.
3. Length: Maximum 3 short paragraphs (under 800 characters total). Be brief and precise.
4. Structure:
   - Paragraph 1: Greeting, interest in the specific position, and 1 sentence summarizing why the candidate's core background fits.
   - Paragraph 2: Direct connection between candidate's past achievements/skills and the key requirement of the job. Show business value.
   - Paragraph 3: Brief sign-off expressing readiness for an interview or technical discussion.
5. Tone: Professional, confident, respectful, avoiding generic corporate cliches ("коммуникабельный", "динамично развивающаяся компания").

Output ONLY the raw text of the cover letter ready to be sent. Do not add any introductory or concluding remarks from yourself.

```

**Промпт пользователя (User):**

```text
Job Title: {Job.title}
Company: {Job.companyName}
Job Requirements Summary: {Job.skills} + Key tasks from description: {Job.descriptionText}
Selected Candidate Profile Summary: {Profile.resumeText}
Additional Instructions from Candidate: {Profile.customPromptInstructions}

```

---

## 11. Security & Privacy Model (Безопасность данных)

* **Принцип наименьших привилегий (MV3 Permissions):**
В файле `manifest.json` запрашиваются минимально необходимые доступы:
* `storage` — для сохранения конфигураций расширения.
* `activeTab` — доступ к содержимому вкладки предоставляется *только* в момент явного открытия пользователем страницы вакансии (снижает уровень тревожности ИБ-систем Chrome Web Store).
* `host_permissions` ограничиваются строго пулом целевых адресов: `*://*.hh.ru/*`, `*://*.hh.ua/*`, `https://api.openai.com/*`, `https://api.deepseek.com/*`, `https://openrouter.ai/*`.


* **Безопасность API-ключей:** Ключи пользователей сохраняются исключительно в `chrome.storage.local`. Синхронизация через `chrome.storage.sync` отключена, чтобы исключить передачу сырых токенов авторизации через облачные сервера синхронизации Google.
* **Минимизация данных перед отправкой в LLM:**
Скрипт контента извлекает только значимый текст. Исходный HTML-код страницы полностью очищается с помощью регулярных выражений или легковесных парсеров от лишних скриптов, рекламных блоков, сайдбаров HH.ru и футеров. Персональные данные пользователя (кроме тех, что он осознанно внес в локальный менеджер профилей) никогда не передаются в LLM.
* **Защита n8n Webhook:** payload вебхука отправляется методом `POST`. Расширение предоставляет поле ввода для кастомизируемого секретного заголовка (например, `X-CareerSignal-Token` или `Authorization: Bearer <token>`), позволяя пользователю защитить приемник своего инстанса n8n от стороннего спама.

---

## 12. Risks and Mitigations (Противодействие антибот-системам)

HH.ru защищен комплексами Qrator Labs и жестко пресекает автоматический парсинг (парсинг страниц без участия пользователя быстро приводит к капче или временному бану по IP).

| Риск | Причина | Стратегия предотвращения (Mitigation) |
| --- | --- | --- |
| **Блокировка за частые фоновые HTTP-запросы** | Массовый фетчинг страниц вакансий из поисковой выдачи через `fetch(url)` из расширения. | **Полный запрет фонового парсинга.** Расширение извлекает данные *исключительно* в момент, когда пользователь физически открыл вкладку вакансии. Нет фоновых сетевых запросов к HH — нет триггеров для антифрода. |
| **Поломка парсера из-за обновлений верстки** | Изменение CSS классов, верстка структуры блоков, внедрение обфускации классов (A/B тесты HH). | **Парсинг глобального состояния.** Вместо DOM-элементов расширение считывает контент скрипта `<script id="hh-state" type="application/json">`. Структура JSON-данных бэкенда стабильна и меняется крайне редко. |
| **Детекция эмуляции кликов антифродом** | Попытка программно нажать кнопку отклика через `document.querySelector('.apply-btn').click()`. | **Исключение автоматических кликов.** Процесс разделен: соискатель сам нажимает нативную кнопку отклика. Расширение лишь перехватывает появление формы через `MutationObserver` и подставляет текст в буфер или поле ввода. |
| **React State Desynchronization** | Прямая программная вставка текста в `textarea.value = text` приводит к тому, что внутренний React-стейт формы HH.ru не видит изменений, и кнопка отправки остается заблокированной. | **Симуляция нативных событий.** Вставка текста производится через цепочку событий: `textarea.focus()`, программная запись в буфер и вызов `document.execCommand('paste')` или через последовательную генерацию `InputEvent` с флагами `{ bubbles: true }`. |

---

## 13. Тест-план (QA Strategy)

### 1. Модульное тестирование (Unit Tests - Vitest)

* Тестирование чистой функции rule-based скоринга: подача различных конфигураций профилей и объектов вакансий (валидация граничных значений зарплат, подсчет совпадений навыков).
* Тестирование парсера JSON-состояния: подача слепков тега `hh-state` для верификации корректности извлечения полей `title`, `description`, `skills`.

### 2. Фикстуры DOM-дерева (Integration Tests)

* Сохранение локально в репозиторий 5-10 реальных HTML-страниц вакансий HH.ru различных типов (с открытой зарплатой, скрытой зарплатой, архивных, без указанных навыков). Поддержание авто-тестов, проверяющих, что парсер безошибочно обрабатывает каждую статическую фикстуру.

### 3. Сквозное тестирование (E2E Tests - Playwright)

* Скрипт инициализирует чистый инстанс Chromium с автоматически загруженным расширением CareerSignal.
* Переходит на мок-страницу, имитирующую структуру HH.ru, проверяет успешность инжекции виджета Shadow DOM, эмулирует клик по кнопке генерации ИИ и проверяет заполнение локальной базы данных Dexie.

---

## 14. Roadmap разработки для одного разработчика

```
[ Неделя 1 ] ──> Setup, Архитектура MV3/WXT, Парсер JSON-state, База данных Dexie
[ Неделя 2 ] ──> Разработка Shadow DOM панели, Алгоритм Rule-Scoring, Менеджер профилей
[ Неделя 3 ] ──> Интеграция API LLM (BYOK), Промпт-инжиниринг, Генератор Cover Letter
[ Неделя 4 ] ──> Подключение вебхуков n8n, Обработка буфера/форм, Финальное тестирование и релиз

```

* **Неделя 1: Каркас системы и извлечение данных**
* Инициализация проекта через WXT (TypeScript + React/Solid).
* Реализация скрипта контента `job-page.ts`, логика извлечения JSON данных из тега `#hh-state`.
* Проектирование локальной БД Dexie.js, описание схем для `jobs`, `companies`, `profiles`.


* **Неделя 2: Интерфейс инжекции и Rule-based логика**
* Создание изолированного Shadow DOM слоя и рендеринг UI боковой панели (Sidebar) с использованием TailwindCSS.
* Написание и покрытие тестами локального компонента Rule-based скоринга.
* Разработка интерфейса создания и редактирования Профилей Кандидата на Options Page.


* **Неделя 3: Интеграция с ИИ (Бизнес-ценность)**
* Реализация безопасного проксирования запросов к OpenAI / DeepSeek через Service Worker (`background.ts`).
* Интеграция JSON-анализа вакансии и рендеринг блоков плюсов/минусов.
* Имплементация логики генерации сопроводительного письма, отладка промптов на реальных вакансиях.


* **Неделя 4: Автоматизация, вебхуки и стабилизация**
* Добавление функционала отправки интеграционных событий на внешние n8n URL-адреса.
* Реализация механизма перехвата формы отклика на HH.ru и интеграция сгенерированного письма через буфер обмена/события ввода.
* Проведение ручного тестирования, отладка краевых багов, сборка релизного zip-архива для Chrome Web Store.



---

## 15. Список конкретных улучшений для добавления в ТЗ

Для минимизации неопределенности в процессе разработки рекомендуется внести в текущее техническое задание следующие дополнения:

1. **Строгое требование к изоляции UI:** Инкапсулировать весь инжектируемый интерфейс расширения внутри `Shadow Root`. Запретить прямое внедрение элементов в основной DOM страницы HH.ru, за исключением четко определенных точек для бейджей в поисковой выдаче.
2. **Декларация метода извлечения данных:** "Основным источником данных о вакансии считать JSON-объект, извлекаемый из контекста страницы (`window.__INITIAL_STATE__` или аналогичного скрипта данных). Традиционный DOM-парсинг через селекторы элементов использовать исключительно как резервный отказоустойчивый канал (Fallback-метод)".
3. **Оптимизация ИИ-потока:** Добавить поддержку потоковой передачи текста (Streaming / Server-Sent Events) для вывода сопроводительного письма в реальном времени. Это существенно повысит субъективную скорость работы интерфейса для пользователя (UX Perception).
4. **Управление версионированием локальной БД:** Внедрить обязательное декларирование версий Dexie.js с первого дня (`db.version(1).stores(...)`). Любые последующие изменения в структурах моделей данных (при переходе к MVP-2/3) должны сопровождаться явными функциями миграций, чтобы не стереть накопленную историю откликов пользователей.
5. **Политика повторных попыток (Retry Policy):** Для сетевых запросов к n8n вебхукам реализовать в Service Worker очередь отложенной отправки со стратегией экспоненциального бэк оффа (Exponential Backoff) на случай, если локальный инстанс n8n соискателя временно недоступен.
"""

with open("careersignal_hh_copilot_mvp_spec.md", "w", encoding="utf-8") as f:
f.write(spec_content)

print("File generated successfully: careersignal_hh_copilot_mvp_spec.md")

```
Ваш Markdown файл (.md) с технической спецификацией и продуктовой стратегией готов!

[file-tag: code-generated-file-0-1781865271566362863]

Я детально расписал все пункты: от архитектурных инсайтов (использование `window.__INITIAL_STATE__` и `Shadow DOM`) до полного плана разработки и TypeScript-интерфейсов. Документ отлично подойдет как готовое ТЗ для разработчика.

```
