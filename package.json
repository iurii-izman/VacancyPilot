{
  "name": "vacancy-pilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build",
    "build:firefox": "wxt build -b firefox",
    "zip": "wxt zip",
    "zip:firefox": "wxt zip -b firefox",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "verify": "pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm verify:aops-workflow",
    "test:release": "pnpm build && vitest run --config vitest.release.config.ts src/release-safety",
    "companion:format-check": "uv run --project companion --extra dev ruff format --check companion/",
    "companion:format": "uv run --project companion --extra dev ruff format companion/",
    "companion:lint": "uv run --project companion --extra dev ruff check companion/",
    "companion:typecheck": "uv run --project companion --extra dev mypy companion/app/",
    "companion:test": "uv run --project companion --extra dev pytest companion/tests/ -v",
    "companion:openapi": "uv run --project companion python -m app.openapi",
    "companion:openapi-check": "uv run --project companion python -m app.openapi --check",
    "verify:companion": "pnpm companion:format-check && pnpm companion:lint && pnpm companion:typecheck && pnpm companion:test && pnpm companion:openapi-check",
    "verify:aops-workflow": "node scripts/verify-application-ops-workflow.mjs",
    "verify:all": "pnpm verify && pnpm verify:companion"
  },
  "dependencies": {
    "dexie": "^4.4.5",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/chrome": "^0.2.7",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "@vitest/coverage-v8": "^3.2.6",
    "@wxt-dev/module-react": "1.2.2",
    "eslint": "^10.9.1",
    "fake-indexeddb": "^6.2.5",
    "happy-dom": "^20.11.12",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.68.0",
    "vitest": "^3.2.6",
    "wxt": "0.21.4"
  }
}
